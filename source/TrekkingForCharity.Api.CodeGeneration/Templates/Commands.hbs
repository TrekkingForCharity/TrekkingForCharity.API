namespace TrekkingForCharity.Api.Client.Commands
{
    public class {{Name}}
    {
        public {{Name}} (
{{#each Props}}
            {{DataType}} {{CamelCaseName}}{{#if @last}} {{else}},{{/if}}
{{/each}}){
{{#each Props}}
            this.{{Name}} = {{CamelCaseName}};
{{/each}}
        }

{{#each Props}}
        public {{DataType}} {{Name}} { get; }

{{/each}}
    }
}